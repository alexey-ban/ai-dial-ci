name: 'Say hello'
description: 'some desc'
inputs:
  hello_world:
    description: 'echo '
    required: false
    default: 'Oh my gosh'

runs:
  using: "composite"
  steps:
    - name: Checkout project
      uses: actions/checkout@3df4ab11eba7bda6032a0b82a6bb43b11571feac #v4.0.0
      with:
        lfs: true
    - name: ls project
      shell: bash
      run: |
        ls -la .
    - name: Test
      shell: bash
      run: |
        echo ${{ inputs.hello_world }}
